cont = y, height


scrollbar_height = (
        cont.y-8
) + (
        cont.height/
        sum(c.rect.height + self.gap for c in self.children)
)
scrollbar_y = cont.y + self.sidebar_scroll

scrollbar_rect = pygame.Rect(
    0,  # 10px ancho de scrollbar desde el borde derecho
    scrollbar_y,
    8,  # ancho del scrollbar
    scrollbar_height
)
